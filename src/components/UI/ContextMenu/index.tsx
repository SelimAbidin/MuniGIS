import * as React from "react";

const ContextMenu = ({children,id}) => (<div id={id}>
                                            {children}
                                        </div>)
export default ContextMenu;